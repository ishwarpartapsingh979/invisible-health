# 1. functions-framework: Google's library to run this code as a Cloud Function
functions-framework==3.4.0
# 2. google-cloud-aiplatform: The official SDK for Gemini (Vertex AI) on Google Cloud
google-cloud-aiplatform==1.60.0
# 3. supabase: To talk to your database (Memory)
supabase==2.13.0
# 4. python-dotenv: To read secrets from .env file when testing locally
python-dotenv==1.0.0
# 5. requests: For making HTTP calls (e.g., to Google Maps API later)
requests==2.31.0
# 6. langfuse: For Observability (upgraded from 2.30.0 to allow packaging 24.x)
langfuse==2.60.3
# packaging: 24.x satisfies wheel>=0.46.3 (requires >=24.0) and langfuse 2.60.3 (requires <25.0,>=23.2)
packaging==24.0
# 7. mcp: Model Context Protocol SDK for integrating with Swiggy, Zepto, Zomato
mcp==1.1.2
# 8. httpx: Async HTTP client for MCP server communication
httpx==0.28.1
# 9. openai: For Whisper audio transcription API
openai==1.59.5
# 10. pydantic: For JSON schema validation
pydantic==2.10.5
# 11. pydub: For audio format conversion to m4a
pydub==0.25.1
# 12. aiohttp: Async WebSocket server for Gemini Live streaming
aiohttp==3.9.3
# 13. google-genai: New Gemini SDK with Live API support
google-genai>=0.3.0
# 14. aiohttp-cors: CORS support for aiohttp
aiohttp-cors==0.7.0
# 15. google-generativeai: Standard Gemini API for transcription/image analysis
google-generativeai==0.8.0

# --- Voice tab (LiveKit worker + token server) ---
# Used by voice_agent.py (speech-to-speech via OpenAI Realtime) and
# voice_token_server.py. Persistent worker / local token server — not the
# run-agent Cloud Function, but kept here per project convention.
# NOTE: livekit's openai plugin may need a newer `openai` than the ==1.59.5
# pinned above. If pip reports a conflict, bump line 20 to the version livekit
# requests and re-test the Whisper transcription path.
# 16. livekit-agents (+openai plugin): LiveKit Agents framework + OpenAI Realtime
livekit-agents[openai]~=1.0
# 17. livekit-api: mint LiveKit join tokens for the iOS app
#     (>=1.0.7 required by livekit-agents 1.x)
livekit-api>=1.0.7,<2
# 18. fastapi: token server HTTP app
fastapi~=0.115
# 19. uvicorn: ASGI server to run the token server
uvicorn[standard]~=0.30
