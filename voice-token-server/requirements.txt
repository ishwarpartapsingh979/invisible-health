# Minimal deps for the Cloud Run token server. No livekit-agents (and therefore
# none of its aiohttp/openai conflicts) — this service only mints tokens.
livekit-api>=1.0.7,<2
fastapi~=0.115
uvicorn[standard]~=0.30
google-genai~=1.0        # Gemini vision — reads schedules from image/video/pdf
python-multipart~=0.0.9  # so FastAPI can accept the uploaded file
